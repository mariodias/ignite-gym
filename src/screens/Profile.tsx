import { Center, Text } from '@gluestack-ui/themed';

export function Profile(){
  return (
    <Center flex={1}>
      <Text color="$white" fontSize="$xl" fontWeight="bold">Profile</Text>
    </Center>
  );
}